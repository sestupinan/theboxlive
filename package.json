{
  "name": "backend",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start-client": "cd front && npm start",
    "build": "npm build && (cd front && npm install)",
    "start": "nodemon ./bin/www",
    "heroku-postbuild": "cd front && npm install && npm run build"
  },
  "dependencies": {
    "cookie-parser": "~1.4.4",
    "cors": "^2.8.5",
    "debug": "~2.6.9",
    "express": "~4.16.1",
    "joi": "^17.3.0",
    "mongodb": "^3.6.2",
    "morgan": "~1.9.1",
    "nodemon": "^2.0.6"
  }
}
